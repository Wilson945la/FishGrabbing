# 2026-08-06 UNO 在线模式收尾

## 完成
- 客户端 ServerClient 连接地址改成本地测试服 `127.0.0.1:8899`（原生产 `172.16.162.87:80` 无 UNO 指令，已注释保留）。
- 重新编译+部署客户端的 `fishgrab.jar`（21MB，含 UnoMatchRoom / UnoGame.showDrawDecideDialog / chooseOnlineColor / applyServerState 等在线类），旧版备份 `fishgrab.jar.bak_20260806_084036`。
- 本地测试服务端重启于 8899（PID 14156，FishGrabbingServer.jar 含 UNO 引擎 + 摸牌决策机制）。
- 写了无头双进程自测 `simtest/UnoOnlineSim.java`（用真实 ServerClient 发指令 + 独立推送 socket 收 UNO_STATE/DUEL_GAME_START），跑完整局：建房→加2机器人→乙加入→双方准备→DUEL_GAME_START→双方收 UNO_STATE→出牌/罚摸(pending=4)/摸牌决策(play+pass 两分支都覆盖)/无卡死。证明在线链路端到端可用。
- 启动桌面客户端（PID 5592/6544）供用户测。

## 关键事实
- 卡牌编码：数字牌 3 字符 `色+类型位+数字位`（如 `Y99`=黄9、`B00`=蓝0）；动作牌 `Gs`(跳)/`Gr`(反转)/`Gd`(+2)/`Kw`(万能)/`Kf`(+4)。
- 推送走独立 socket：`PUSH_REGISTER|user` → `PUSH_OK`，之后收 `PUSH|sender|msg`；与 ServerClient 请求-响应 socket 分离，无半双工串读问题。
- `+4 质疑`：服务器 `pendingChallengeAgainst=order.get(currentPlayerIdx)`，当前玩家须先 `unoChallenge(roomId,me,accept)` 才能继续出牌（否则 `ERROR|请先处理 +4 质疑`）。

## 待办（未做）
- 桌面客户端 GUI 端到端手测（用户手动点）。
- 生产服部署：新 FishGrabbingServer.jar 需推到 172.16.162.87 才支持远程多人；并把 ServerClient 切回生产地址。
- UNO 喊/抓在线同步、方向环/动画在线态、机器人加满策略联调（引擎已支持，GUI 未手测）。

## 改：UNO 邀请改为好友列表弹窗（用户要求）
- 之前 UNO "邀请好友" 是 `JOptionPane.showInputDialog` 手填名字；现在改成与其他对决玩法一致的**好友列表弹窗 + 点名字直接邀请**。
- `UnoMatchRoom.inviteFriend()`：弹 `JDialog` 列出 `ServerClient.getFriends` 返回的好友（排除 moyu官方 与 自己），点选后 "邀请" 按钮触发 `sendDuelInvite`。
- `sendDuelInvite`：发 `DUEL_INVITE:roomId:mode:maxPlayers:UNO:ts`（修正原格式——原把游戏类型错放在 mode 位），成功后再 `MessageCenter.openAndNavigate` 打开与该好友的聊天。
- `UnoMatchRoom.joinRoom` 签名改为与其他玩法一致：`(username, userId, roomId, mode, maxPlayers)`，改用静态 `globalHome`/`globalUnoHome`（构造时赋值），与原 `UnoHome` 入口一致。
- `MessageCenter.createDuelInviteCard` 接受分支补 `else if ("UNO".equals(duelGame)) UnoMatchRoom.joinRoom(...)`；邀请卡片文案对 UNO 模式做映射（0=普通叠加 /1=逆转叠加）。
- 已重编译、部署 `fishgrab.jar`（备份 `fishgrab.jar.bak_20260806_090512`），桌面客户端重启（PID 3276/3288）。
- 注：UNO 在线房目前连本地 8899（非生产服），故好友邀请要真能送达，对方也须连同一服务端；远程联机仍需部署生产服+切回生产地址。

## 修：PUSH 推送连错地址（导致邀请和开局都收不到）
- 用户反馈：4/4 都准备了但游戏没开始 + 好友收不到 UNO 邀请。
- 服务端日志显示：`[推送] 无会话，无法推送给 moyu | DUEL_INVITE:1005:...` / `DUEL_GAME_START:1005:...`。
- 根因：`MessageCenter.PUSH_HOST="172.16.162.87",PUSH_PORT=80`（生产服），而 `ServerClient.HOST="127.0.0.1",PORT=8899`（本地服）。两条路不同。
  - 请求-响应 → 本地 8899 ✅
  - PUSH 推送 → 生产 80 → 本地 pushClients 没注册 → 找不到会话 → 推不出去
- 修复：`MessageCenter.PUSH_HOST="127.0.0.1",PUSH_PORT=8899`，与 ServerClient 保持一致。
- 服务端 PUSH_REGISTER 也在 8899 端口（handleConnection 看到 `PUSH_REGISTER|xxx` 就 long-poll 维持长连接）。
- 重编译+部署（备份 `fishgrab.jar.bak_20260806_091456`），启动新客户端 PID 19880 待用户登录验证。

## 修：UNO 玩家位置布局重叠（看不到我）
- 4 人局，房间 1006，玩家顺序 [moyu, moyu2, 机器人10, 机器人11]。
- moyu2 视角（myPlayerIdx=1）下：moyu 头像消失，右中位置空着。
- 根因：`UnoGame.computePlayerPositions` 用 `orderIdx = (i - myPlayerIdx - 1 + others) % others`（`others = n-1`）。
  - i=0 (moyu) → (0-1-1+3)%3=1（顶中）
  - i=3 (机器人11) → 4%3=1（顶中）—— 跟 moyu 撞了！都被画到顶中。
  - 实际 moyu 应在右中 (orderIdx=2)，被覆盖。
- 修复：用条件分支正确算"i 是第几个非我玩家"（turn 顺序：i 在 myPlayerIdx 之后排前面，否则排末尾）。`others` 仍只用于 step。
- 编译部署：备份 `fishgrab.jar.bak_20260806_092336`，新 jar 已替换。两端客户端需重启才能看到修复。

## 修：摸牌堆"摸牌 / 0 张"误导
- 用户截图反馈：摸牌按钮下显示"0 张"，以为摸牌堆空了。
- 根因：客户端 `UnoGame.drawDrawPile` 画的是本地 `drawPile.size()`（离线用的 LinkedList<UnoCard>），在线模式根本不维护这张堆——服务端权威从未推送剩余张数。
- 修复：抹掉"X 张"那一行，只留"摸牌"字样，把牌背堆叠效果当主要视觉指示。线粗 13 略放大居中。
- 摸牌数真正显示位是玩家头像的 `+ N` 红色字（pendingDrawsOnMe），不变。
- 编译+部署：备份 `fishgrab.jar.bak_20260806_093459`。重启 PID 10756/18476。
- 服务端 drawCards() 有 reshuffle 兜底（drawPile 空就重洗弃牌堆），所以即使后端 0 张也不至于卡死——只是客户端没显示这个数据。

## 改：被加牌且无可接牌时自动认罚（不用点摸牌）
- 用户需求：被 +2/+4 加了牌后，如果自己手里没有 +2/+4（或 mode=1 下无可接的同色牌）可以打出，就直接吃下累加张数，而不是还要点"摸牌"按钮。
- 实现：`UnoGame.applyServerState` 末尾加判断：当 `pendingDraws>0 && 当前是我回合 && 无未决 +4 质疑 && 无摸到可出牌待决策 && !canStack(myPlayerIdx)`（本地无牌能 canPlayOn）→ 自动 `SwingUtilities.invokeLater(onDrawClicked())`，在线模式 onDrawClicked 会走 `ServerClient.unoDraw` 吃 pendingDraws。
- 防重复：加 `autoDrawPending` 标志，触发一次后置 true；当 `pendingDraws==0 || 轮到别人` 时重置，避免同一状态被反复推时重复吃牌。
- 有牌可接时不自动（让玩家自己选接或吃）。
- 编译部署：备份 `fishgrab.jar.bak_20260806_09xxxx`（见 ls），重启客户端 PID 5644/7704。

## 改：抓 UNO 锤子机制 + 结算动画等待
- 在线/离线两版的锤子机制差异：
  - 离线：客户端自己推导 showHammer = (i != myIdx) && cnt == 1 && !calledUno。
  - 在线：之前完全没推导，导致用户截图看不到锤子。
- 用户需求：
  1. 锤子按钮要够大（不是三角角标那种 18px）
  2. 持续到"下家出牌前"才消失
  3. 点锤没反应

### 服务端 (FishGrabbingServer/src/server/Main.java)
- serialize() 加第 4 字段 per-player：hammer = (hand=1 && !calledUno && P != 当前回合)——保证"轮到他时窗口关闭"。
- 删除未用的 hammerActive 字段（按需计算更准，避免维护漂移）。
- unoEnd 改造：先 gameOver=true + 推最后一手 UNO_STATE 让客户端播最后一张飞出的动画（≈360ms），再 1100ms 后（用 matchScheduler.schedule）推 DUEL_GAME_OVER，让"最后一张飞出去 → 结算框"时序正确。
- unoCatch 不变（已有 hand==1 + !calledUno 校验 +2张）。

### 客户端 (test/src/UnoGame.java)
- applyServerState：解析新增的 hammer 字段；改成 parts = entry.split(",", -1) 数组解析，向前兼容旧两列。
- 在线模式直接用 hammerFlags 权威信号，不再本地推导。
- Player 类加 Rectangle hammerHit 字段。
- 锤子绘制：角标 → 直径 52px 圆按钮（红色 + 白边 + 阴影 + "抓!"白字，闪烁），点击区存到 hammerHit。
- 点击事件：先看 hammerHit，再看头像圆（前者优先）。
- 在线抓 UNO：增加 showAction 提示"抓到 X 没喊 UNO！+2张" + 服务端 SUCCESS 后真的 +2；用 Thread + invokeLater 而不是 sendOnlineCmd（后者吞错信息）。

### 部署/启动
- 服务端编译部署到 FishGrabbingServer.jar，用 bash run_in_background 启动在 80 端口（PID 18952）。
- 杀掉了 IDEA 调试实例 (PID 13292，端口 80)，它加载的是旧 Main.class——我的源码改动没自动生效。
- 客户端 jar 已重打包（备份 fishgrab.jar.bak_20260806_102253），启动 1 个客户端 PID 11312 在登录页，等用户双开第二个。

### 注意事项
- 服务端 jar 一定要用 cfm ... MANIFEST.MF 才有 Main-Class（构建脚本 _build_srv.ps1 已修正）。
- 服务端保持 80 端口（用户偏好"端口: 80"窗口可见）。
- 本地测试服务端重启于 8899（PID 14156，FishGrabbingServer.jar 含 UNO 引擎 + 摸牌决策机制）。

## 修：好友接受邀请进房后退房只剩"信息中心"，不回摸鱼中心
- 现象：好友从消息中心(信息中心)点"接受邀请"进 UNO 房，退房后只留下信息中心窗口，应回到摸鱼中心(FishGrabbingHome)。
- 根因：`UnoMatchRoom.joinRoom`（邀请进房路径）把 `globalHome` 当 parent 传进去；好友从信息中心进房、没从摸鱼中心点过"建房"，所以 `globalHome==null`。退房 `windowClosing` 的 `if (parent != null) parent.setVisible(true)` 被跳过 → 摸鱼中心不回来，只剩一直没关的信息中心。
- 修复：
  - `UnoMatchRoom.joinRoom` 用 `FishGrabbingHome.getActiveInstance()` 兜底拿摸鱼中心实例作 parent，并置 `enteredViaInvite=true`。
  - 新增字段 `enteredViaInvite`；`windowClosing` 在 `!gameStarted` 且 parent 非空时：先 `parent.setVisible(true)` 回摸鱼中心，再 `MessageCenter.closeActiveInstance()` 关掉残留的信息中心。
  - `MessageCenter` 新增静态 `closeActiveInstance()`（dispose activeInstance 并置 null）。
- 注：`MessageCenter.hideActiveInstance()` 只隐藏不销毁 Home，所以 getActiveInstance() 在会话内始终有效；Home 的 windowClosing 才 System.exit(0)。
- 编译部署 fishgrab.jar（备份 fishgrab.jar.bak_20260806_110351），重启客户端。


## 锤子三连改 + 服务端发布怪现象 (11:21+)
- 客户端 (UnoGame.java)：删掉 8s 强制过期 + 下家出牌清上家锤子 + 自己回合不能抓 → 改为始终不透明、连续高闪、被抓者自己看不到、任意人（含下家自己刚出完牌）都能抓。
- 服务端 (FishGrabbingServer/src/server/Main.java)：advance() 增加保存 oldIdx、关闭 prev(actor) 的 `hammerClosed`，actor 自身 remove；serialize 的 canCatch 加 `&& !hammerClosed.contains(name)`；unoEnd 增加统一重置 gameStarted=false 与所有玩家 ready=false（修了"房主先准备要取消再准备"）。
- 发布怪现象：**80 端口一直被 IntelliJ IDEA 的 Run Configuration 拉起的 `server.Main`（带 idea_rt.jar 探针）占着**，我之前所有"重新编译+重启"实际上只生成了新 jar，但 Start-Process 失败（端口已被占），老 IDE 进程继续服务。我之前以为是"deploy 成功"其实是错觉。
- 当前的真相：需要先 Stop-Process PID（看 Get-Process / Get-CimInstance 是否有 idea_rt.jar 标志来识别），再 Start-Process jar。
- 现在 80 端口上是真正的最新 jar：PID 6676（11:45 启动），含 hammerClosed 逻辑 + REVERSE 翻转清空 hammerClosed + 每步 advance 打印 `[uno] advance ...` 日志（用于下次复现时定位）。
- 教训：发布服务器 jar 之前必须先用 `Get-CimInstance Win32_Process` 检查端口 80 占用者是不是 `idea_rt.jar`；是的话，**先杀 IDEA 那个**，再启动 jar。

## 完成：5 个计时/UI 问题（13:38 发布）
用户提 5 项，全部改完并重新编译部署：
1. **时间前符号不显示** → `drawTopBar` 用 Graphics 画手绘小时钟（同心圆+指针+中心点）替代不渲染的 emoji ⏱。
2. **时间到不结算** → 服务端 `scheduleGameEnd(matchDurationMs)` 到点调 `unoEnd`→推 `DUEL_GAME_OVER`；客户端 `receiveGameOver`→`applyGameOver` 弹排名框并 dispose。在线模式 `onTick` 早退（结算靠服务端推送）。
3. **总时长 4 人 10min、每多 1 人 +2min** → 服务端 `UnoMatch` 构造 `matchDurationMs=(10+max(0,n-4)*2)*60000`；`serialize` 末尾追加 `|matchStartMs|matchDurationMs|turnDeadline`；客户端 `applyServerState` 解析这三字段，`remainingMs()` 在线走 `srvMatchStartMs+srvMatchDurationMs`。
4. **出牌限时 15s 自动代打** → `TURN_LIMIT_MS=15000`；`scheduleTurnTimer` 超时调 `autoPlayOrDraw`：先打能出的牌（非万能优先，再万能），无牌则摸 1 张、摸到能出就直接打（pendingDraws>0 则直接认罚吃牌）。`unoPlay`/`unoDraw`/`unoDrawDecide` 行动后都重新排回合定时器。
5. **质疑 15s 自动不质疑 + 万能牌变色 15s 自动选最多色** → 服务端 `scheduleUnoChallengeResolve` 9s→15s；客户端三个弹窗（`showOnlineChallengeDialog` 质疑、`promptColorPicker` 正常出万能、`chooseOnlineColor` 摸到万能）都加 15s `javax.swing.Timer`：质疑超时→自动不质疑并关；选色超时→`mostHandColor()`（统计四色手牌数取最大，平局 `new Random` 随机选）。
- 客户顶栏加"出牌 Ns"回合倒计时（用 `srvTurnDeadline` 实时算），<5s 变红。倒计时 Timer(500ms) 持续 repaint 保证刷新。
- **部署**：客户端编译 `out/`→`fishgrab.jar`（备份 `fishgrab.jar.bak_20260806_133814`），拷贝到 `FishGrabbingAssistant/app/fishgrab.jar`。服务端 `javac -cp lib/mysql-connector...` 编 `src/server/Main.java`→`out/production/FishGrabbingServer`→`jar cfm FishGrabbingServer.jar MANIFEST.MF ...`。杀掉 IDEA 实例 PID 21036（占 80 加载旧 out/），Start-Process 新 jar → **PID 14396** 监听 80，srv_out/srv_err 日志空。
- 验证：逐项 grep 源码确认字段/解析/弹窗计时/autoPlayOrDraw 全到位；netstat :80 仅 14396 LISTENING。

## 离线 UNO 规则对齐在线 (14:10 发布)
用户要求"离线模式 UNO 规则改得和在线模式一样"。改 UnoGame.java（纯客户端，无需服务端），编译部署 fishgrab.jar（备份 fishgrab.jar.bak_20260806_141058）。
1. **总时长**：离线从静态 6 分钟改为与在线同公式 `gameDurationMs=(10+max(0,n-4)*2)*60000`（n=玩家数，构造时算）；`remainingMs()` 离线分支改用 gameDurationMs；原 `TIME_LIMIT_MS` 常量改名 `TURN_LIMIT_MS=15000`（出牌限时）。
2. **人类回合 15s 出牌限时**：`beginTurn` 人类回合调 `startTurnTimer()`（15s javax.swing.Timer），超时 `autoPlayOffline()` 代打（非万能优先→万能→否则摸1张、摸到能出就打、被加牌则直接认罚）；顶栏复用 `srvTurnDeadline` 显示"出牌 Ns"；出牌/摸牌/选色/质疑时 `stopTurnTimer()`；bot 回合与每回合开始清 srvTurnDeadline。
3. **+4 质疑 15s**：抽出通用 `confirmTimeout(title,html,yes,no,default)` 15s 框（超时按 default）；离线 `JOptionPane` 质疑改 `confirmTimeout(default=不质疑)`；在线 `showOnlineChallengeDialog` 也复用同一框，逻辑不变。
4. **摸牌"是否打出"提示**：离线 `onDrawClicked` 的 `JOptionPane` 也改 `confirmTimeout(default=不放)`，与在线 drawnPlayable 超时语义一致。
5. **锤子对齐在线**：`beginTurn` 清当前玩家 `showHammer`（对齐在线 `!current turn`）；`checkHammerExpiry` 去掉 8s 强制过期，改为持续（直到被抓/轮到/出牌消失）。绘制层 `idx!=myPlayerIdx` 隐藏自身锤子已与在线一致。
- 注意：离线单人对局里只有人类是"人类"，bot 自动喊 UNO 不挂锤，所以离线实际看不到他人锤子（与在线多人场景不同，属必然差异）。
- 编译 javac 报 OK，jar 部署到位。

## 改：多人布局改三段（上/左/右） (14:27 发布)
- 用户截图 8 人上半圆太挤。改 `UnoGame.computePlayerPositions` 不再走均匀上半圆，改成按人数把其他玩家分到 top/left/right 三段。
- 规则（含自己，总人数 n）：
  - 8→上3左2右2；7→上2左2右2；6→上2左2右1；5→上2左1右1；4→上1左1右1（推断）；3→上1左0右1（兜底）；2→上0左0右1（兜底）。
- 实现：top 段 y=80、x 在 220-780 均匀铺开；left 段 x=200；right 段 x=800；left/right 段 y 在 PLAY_AREA_CY±120 均匀分布；区段只有 1 人时居中。
- 顺序：先填 top，再 left，再 right，按 turn 顺序（i > myPlayerIdx 取前序；否则末尾续）排入。
- 在线/离线共用同一份（`computePlayerPositions` 与模式无关）。
- 缓存：`UnoMatchRoom`/服务端 `applyServerState` 路径已有 `playerPositions = null` 兜底；离线 `UnoGame` 构造时 playerPositions 默认为 null，第一次绘制会算。
- 部署：javac OK；fishgrab.jar 已重打包部署（备份 fishgrab.jar.bak_20260806_142719）。

### 多人布局坐标微调 (14:33)
- 用户截图反馈 8 人局：① 顶段 y=80 头像顶部 44 几乎贴顶栏 36；② 顶段两端（slot 0,2）的牌背堆 y=150-192，跟左/右段 slot=0 头像 y=200±36（164-236）x 范围重叠、y 几乎贴边，视觉上撞到。
- 调 `seatForRegion`：
  - 顶段 y 80→110（避顶栏，留 24px 间距）；spanX 560→500（两端头像 x 由 220/780 收到 250/750）。
  - 左/右段 y 范围 [PLAY_AREA_CY-120, PLAY_AREA_CY+120] → [270, 420]（PLAY_AREA_CY+25=345 居中，spanY 240→150），整体下移，跟顶段牌背堆 y 错开；最低点 420 离自己头像顶部 534 还有 110+px，给手牌区留出位置。
- 验证（8 人，top 3+left 2+right 2）：top 牌背 y=180-222 vs left slot=0 头像 y=234-306，y 错开 12+px；left slot=1 牌背 y=470-512 vs 自己头像 y=534-606，y 错开 22+px；都不真撞。
- 部署：javac OK；fishgrab.jar 重打包（备份 fishgrab.jar.bak_20260806_143352）。

### 名字/牌背按 region 分层避重叠 (14:38)
- 用户截图反馈 8 人局：左/右段玩家（"机器人4" 等）的名字在头像下方（ay<PLAY_AREA_CY → nameBelow=true），跟头像下方的牌背堆 baseY 公式 `ay+AVATAR_R+34` 只差 19px，中文 13 号字实际占 ~16-18px 高度，视觉上贴边甚至压在牌背顶部。
- 修复：按 region 决定 nameBelow/牌背 baseY，**顶段名字→头像下方+牌背再让 30px；左/右段名字→头像上方，牌背紧贴头像下方**。
  - `Player` 加 `seatRegion` 字段（"top"/"left"/"right"/"self"），`computePlayerPositions` 末尾填充。
  - `drawPlayer` 中 `nameBelow = "top".equals(reg)`；牌背 `baseY = ay + AVATAR_R + (nameBelow ? 44 : 10)`（顶段留够 30px 给名字，左/右段牌背紧贴头像下方，因为名字已经上去不挤）。
- 配套调整 `seatForRegion`：left/right 段 y 范围 [270,420] → [305,435]（spanY 150→130，居中 PLAY_AREA_CY+50=370），整段下移：顶段牌背底 y≈232 → left slot=0 名字顶 y≈262，留 30px；left slot=1 牌背底 y≈525 → 自己头像顶 y=534，留 9px。
- 验证（8 人）：顶段名/牌背间距 25px，左/右段名/牌背一上一下完全错开，跨段 y 错开 ≥14px，无重叠。
- 部署：javac OK；fishgrab.jar 重打包（备份 fishgrab.jar.bak_20260806_145505）。
- 右列 slot 镜像：左列 slot0=下、slot1=上；右列改成 slot0=上、slot1=下。沿桌走（C-C-W，player→左→顶→右→player）严格匹配 turn 顺序。
- 15:09 新增出牌提示闪烁：轮到玩家(离线/在线均)且有能出的牌→手牌里所有可出牌亮春绿色(BLINK_GLOW 0x8cff5a)填充+描边，alpha 随 blink() 周期 650ms 平滑脉冲；若一张能出的都没有→摸牌堆外发光描边+柔光填充闪烁。blink 由 spinTimer(45ms repaint)驱动，无需新计时器。myTurnNoPlayable() 判无牌可出。编译 OK，重打包 fishgrab.jar（备份 fishgrab.jar.bak_20260806_150928）。
- 15:28 修离线 UNO 退出去向：原 `startOfflineGame` 回调 `parent.setVisible(true)`（FishGrabbingHome=摸鱼中心主页）且 `dispose()` 匹配房，导致退出直接回主页。改为与在线一致：`setVisible(false)` 隐藏匹配房、回调里 `gameStarted=false`+重置开始按钮("开始游戏"/黄底黑字)+`setVisible(true)` 回到匹配房(UnoMatchRoom)可再开一局。编译 OK，重打包（备份 fishgrab.jar.bak_20260806_152800）。
